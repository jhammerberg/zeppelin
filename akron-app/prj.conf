CONFIG_GPIO=y

CONFIG_LOG=y

CONFIG_INIT_STACKS=y

CONFIG_NETWORKING=y
CONFIG_NET_IPV4=y
CONFIG_NET_IPV6=y
CONFIG_NET_UDP=y
CONFIG_NET_TCP=y
CONFIG_DNS_RESOLVER=y
CONFIG_DNS_RESOLVER_AI_MAX_ENTRIES=10

CONFIG_NET_SOCKETS=y
CONFIG_HTTP_CLIENT=y

# Net address configuration. DHCP vs. static is chosen per-board
# (see boards/esp32_devkitc.conf and boards/native_sim.conf).
# Following line must be enabled, otherwise WiFi connection fails with -1.
CONFIG_NET_CONFIG_SETTINGS=y
CONFIG_NET_CONFIG_INIT_TIMEOUT=1

CONFIG_NET_TX_STACK_SIZE=2048
CONFIG_NET_RX_STACK_SIZE=2048

CONFIG_NET_PKT_RX_COUNT=10
CONFIG_NET_PKT_TX_COUNT=10

CONFIG_NET_BUF_RX_COUNT=20
CONFIG_NET_BUF_TX_COUNT=20

CONFIG_NET_MAX_CONTEXTS=10

# Ethernet L2 is required by both targets:
#  - esp32_devkitc: works around a missing #include <ethernet/eth_stats.h>
#    in esp_wifi_drv.c (harmless "No SOURCES given to Zephyr library:
#    drivers__ethernet" CMake warning is expected).
#  - native_sim: backs the emulated eth_native_tap interface.
CONFIG_NET_L2_ETHERNET=y

CONFIG_PRINTK_BUFFER_SIZE=2048
